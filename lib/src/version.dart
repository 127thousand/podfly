/// Package version — keep in sync with `pubspec.yaml`.
const String podflyVersion = '0.11.2';

/// Short one-line identity for `--version` / banners.
String podflyVersionLine() => 'podfly $podflyVersion';
