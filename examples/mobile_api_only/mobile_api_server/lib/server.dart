// Structural sample only — not a runnable Serverpod app.
// No FlutterRoute / web serving → mobile clients hit the API only.

void run(List<String> args) {
  // Serverpod(args, Protocol(), Endpoints()) ...
  // await pod.start();
}
